# AI Handoff

## 現状（2026-07-18：通常UI統合とレビュー修正完了）

### 完了したこと

- `.x` と `.xdf` の通常UIを `ssos/os/app/scene.c` に統合。
  - 3ウィンドウ生成、入力、ドラッグ、差分本文描画、z-orderを共有。
  - `os/app/main.c` はbaremetal側の初期化と `ss_run()` 入口だけに分離。
- モード依存パレットを `os/gfx/palette.{c,h}` に集約。
  - 16色/256色の論理色indexと実機パレット設定を共有。
- main taskの登録を `os/kernel/main_task.{c,h}` に集約。
- 重なった下位ウィンドウの本文が上位へ描画される不具合を、上位windowクリップで修正。
- z-orderを255到達前に再採番するよう修正。
- 通常UIとstandaloneベンチで、3ウィンドウの座標・z・タイトルを
  `SSSceneWindowSpec` として共有。
- `SS_PROFILE_GFX=0` のstandaloneから未使用の旧UI/data_threadを除外。
  - `-bench` のprimitive描画・計測手順は `SS_PROFILE_GFX=1` のstandalone専用として保持。
- READMEに統合範囲と、意図的に共有しない境界を記載。
- `.zcode/` を `.gitignore` に追加。
- main taskの初回復帰判定を強化。
  - 新規taskは `context == stack_base && entry != NULL` の場合だけ
    `.start_task` へ進む。
  - main taskのsentinel状態から最初の `main → worker → main` 往復を、
    cooperative/preemptive両方のQEMUテストで検証。
- `premain.c` の無効化済みパレット実装、standaloneの完全未使用変数・関数、
  未使用の論理パレット値を削除。
- `README.md` と `ssos/README_ja.md` の旧2 task/旧UI記述を、現行設計または
  解消済みの履歴として整理。
- `README.md` のスタンドアロン説明を補正。
  - `-8` / `-16` をそれぞれ256色 / 16色モードとして明記し、共有通常UIと
    `.x` ホスト固有の初期化・終了・ベンチ処理を分離して説明。

### 設計上の意図的な境界

- 通常UIは単一task。旧standaloneの`data_thread`は再導入しない。
  本文更新と描画の並行競合を避けるため。
- `-bench`、`bench.txt`/`runtime.txt`、Human68KファイルI/O、watchdogは
  standalone専用。baremetalへDOS依存を持ち込まない。
- `.x`と`.xdf`の初期化・終了はホスト固有のまま維持。

### 直近コミット

- このhandoffを含む README文書修正コミット。
- `fix: harden main task bootstrap`（このhandoffを含むレビュー修正）。
- `2444929 docs: document UI integration boundaries`
- `6b93b7f refactor: isolate standalone benchmark code`
- `379b3cf refactor: share normal scene fixture with benchmark`
- `9c7f50c refactor: separate shared scene from OS entry`
- `febba6c fix: preserve shared scene palette and z order`
- `30078f2 refactor: share standalone and boot UI scene`

### 検証済み

- `make test`: cooperative/preemptive各74、計148 PASS。
- `make test-qemu`: cooperative/preemptive計8 PASS（main task登録テスト各1本を含む）。
- `SCHED=cooperative|preemptive`、`SS_PROFILE_GFX=0|1` の`.x`/`.xdf`ビルド成功。
- エミュレータで `ssos_cop.x` / `ssos_pre.x`、`ssos_cop.xdf` /
  `ssos_pre.xdf` の通常操作・重なり・ドラッグ・ESC終了を確認。
- `ssos_cop.x -8 -bench 100` の基準値を確認。
  - full=1021, region=65, z-expose=341, text-update=15,
    drag-region=314, xor-move=16。
  - DMA error=0、timeout=0。

### 次回の具体的なステップ

統合プランとレビュー修正は完了。新しい要求がなければ追加の共通化は行わない。
ベンチ実行部の共通化や`data_thread`再導入は、READMEの設計境界を変更する明示的な要求が
ある場合だけ再検討する。
