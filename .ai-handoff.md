# AI Handoff

## 現状（2026-08-11：OS性能・保守性・MC68000安全性のチーム監査完了）

### 完了したこと

- 性能、保守性・可読性、MC68000/X68000低レベル安全性をagent teamで並列監査した。
- ウィンドウ描画で参照されていなかったz-mapを撤去した。
  - 6KiBの常駐RAMを削減した。
  - geometry/z変更後の無駄なmemsetと全window/block走査を削除した。
  - profile、standaloneログ、README、性能文書をz順再合成の現行実装へ統一した。
- Window APIを安全化した。
  - ID検証を`window_by_id()`へ集約した。
  - 破棄済みID、二重破棄、負値・uint16範囲外geometry、NULL文字列を安全に拒否する。
  - 同一zでは後のslotを最後に描画する順序へhit-testのtie-breakを合わせた。
- buddy allocatorの再結合不具合を修正した。
  - XOR基準を管理領域込みarena先頭から実ブロック領域先頭へ変更した。
  - 小block解放後に64KiBへ戻らず、空き容量があっても最大block確保が失敗する偽OOMを解消した。
  - NULL、小さすぎるarena、奇数/不正alignment、加算overflow、最大block超過を拒否する。
- slab allocatorを安全化した。
  - NULL、pointer未満または不整列object size、不整列memory、容量不足、uint16 count overflowを空cacheとして扱う。
- bare-metal割り込み初期化順を修正した。
  - `entry.s`のBSS初期化前`ss_set_interrupts()`を削除した。
  - IOCS処理後の`premain.c`で一度だけ独自MFP/ISRを設定する。
  - cooperative/preemptive両ISRの`rte`直前にあった`move.w #0x2000, sr`を削除し、復帰直前の不要な多重割り込み窓を除去した。
- 境界条件と再現テストを追加した。
  - buddy最大order再結合、allocator不正入力、slab count/alignment、Window geometry/liveness/NULL、同一z hit-testを検証する。
- 監査結果と保留事項を`docs/os-audit-2026-08-11.md`へ記録した。

### 検証済み

- `make verify`成功。
  - Native: cooperative 101 + preemptive 101、計202 PASS。
  - QEMU: cooperative 4 + preemptive 5、計9 PASS。
  - ASM: 5 PASS。
  - `ssos_cop.x`、`ssos_pre.x`、`ssos_cop.xdf`、`ssos_pre.xdf`の4成果物をビルド成功。
- `git diff --check`: 問題なし。
- linkerのBOOT.X RWX warningは既存状態で、今回の変更による失敗ではない。

### 進行中・未確認

- 自動検証とproduction buildは完了している。
- X68000エミュレータまたは実機で次を確認する必要がある。
  - cold boot直後の両scheduler、Timer D/V-DISP発火、保存vectorと復元、mouse入力、ISR非ネスト。
  - DMAC Ch.2のOCR chain指定、BAR/BTC table、MAR/MTC、large rect、timeout/fallback、転送先境界。
  - TRAP #14異常終了後のMFP、COPY/NMI vector、画面、mouse、Human68K状態復元。
  - watchdogのMFP再設定値と排他。
  - mode 8 page flip時のCRTC R12-R19。
- `ss_disable_interrupts()` / `ss_enable_interrupts()`は元SRを保存しないため、ネストcritical sectionには未対応。
- `scene.c`のIOCS入力provider分離とNative scene統合テストは未着手。

### 次回の具体的なステップ

1. X68000エミュレータで4成果物のcold bootとTimer D/V-DISP/mouseをsmoke確認する。
2. large rectangleのDMAC register値と転送結果を記録し、`OCR=0x19`とchain tableの整合を原典・実測で確定する。
3. TRAP #14異常終了専用cleanupを設計し、Human68K復帰後のvectorとUI状態を検証する。
4. `ss_irq_save()` / `ss_irq_restore()`のABIとQEMU SR保存復元テストを先に作り、critical sectionを段階移行する。
5. `scene.c`へinput providerとscene stateを導入し、初期描画、content更新、drag、ESCを複数frame実行するNative統合テストを追加する。

今回のコード、テスト、文書、本handoffは同一コミットに含める。pushは行わない。
