# AI Handoff

## 現状（2026-08-07：スケジューラ共通化・起床処理最適化完了）

### 完了したこと

- cooperative/preemptiveで重複していたスケジューラ本体を
  `ssos/os/kernel/scheduler.c`へ集約した。
  - 方式別ディレクトリには`interrupts.s`と小さな`wakeups.c`だけを残した。
  - 約480行の重複実装を削除した。
- sleep中タスクを専用の単方向リストで管理するよう変更した。
  - 起床処理は全32 TCBではなく、WAIT中タスクだけを走査する。
  - 32-bit tick周回をまたぐ期限判定に対応した。
  - sleep上限を`SS_MAX_SLEEP_TICKS`として明示した。
  - 実行可能な別タスクがない場合は`SS_ERR_STATE`を返し、WAITのまま即時復帰する不整合を防いだ。
- `SSTask`の`pad`を`resume_type`へ改名し、末尾に`sleep_next`を追加した。
  - m68kターゲットでは`_Static_assert`により、アセンブリが使うTCBオフセットを検証する。
  - production/QEMUアセンブリの構造体注記も同期した。
- task作成時の安全性を改善した。
  - custom stackのアラインメントとサイズを検証する。
  - auto stack arena未設定エラーでTCBスロットを消費しないよう修正した。
- work queueの満杯判定とIPC non-blocking receiveの空判定を、割り込み禁止区間内へ移した。
- work queueのNativeテストを追加し、schedulerのtick周回・単独sleep・stack不正・arena未設定テストを追加した。
- `make verify`を修正した。
  - `tools/makedisk`へ誤って`verify` targetを伝播していた問題を修正した。
  - `test-asm`も完全検証へ含めた。
- Makefile、README、AGENTS.md、テスト文書、`verify-check`分類を新構成へ同期した。

### 検証済み

- `make test`: cooperative/preemptive各82、計164 PASS。
- `make test-qemu`: cooperative/preemptive計8 PASS。
- `make test-asm`: 5 PASS。
- `make verify`: 上記テストに加え、以下すべてビルド成功。
  - `ssos_cop.x`
  - `ssos_pre.x`
  - `ssos_cop.xdf`
  - `ssos_pre.xdf`
- `git diff --check`: 問題なし。

### 進行中・未確認

- 自動検証は完了している。
- `verify-check`は`SSTask`レイアウト変更を含むため、4成果物すべてを実機またはX68000エミュレータで確認対象としている。
- MFP/ISRの命令列は変更していない。`interrupts.s`の変更はTCBオフセット注記のみ。

### 次回の具体的なステップ

1. X68000エミュレータまたは実機で`ssos_cop.x` / `ssos_pre.x` / `ssos_cop.xdf` / `ssos_pre.xdf`を起動する。
2. 通常UIの描画、マウスドラッグ、キー入力、ESC終了を確認する。
3. worker taskを使う構成では、sleep後の復帰とcooperative/preemptive双方の切替を確認する。

今回の変更と本handoffは同一コミットに含める。pushは行わない。
