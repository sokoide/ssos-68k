# AI Handoff

## 現状（2026-07-18：SSOSグラフィックス性能改善）

### 完了したこと

- 256色モードのDMACを正常化。
  - CSR完了判定、BFC設定、timeout時SAB停止、偶数アラインメント、RAM→GVRAM方向（`OCR=0x19`）を修正。
  - エミュレータで全DMAが `error=0 timeout=0` を維持。
- Window重なり・ドラッグの描画崩れを修正。
  - 領域再合成、本文の上位Windowクリップ、Activeタイトル修復を実装。
  - 非Active Windowを前面へドラッグした場合も、旧Activeタイトルを非Activeへ戻す。
- `SS_PROFILE_GFX=1`時の`bench.txt`と通常運転時の`runtime.txt`を実装。
  - 固定`drag-region`ベンチを追加。hide → 旧領域再合成 → XOR → move/show → 新領域再合成を測定する。
- ドラッグ描画を最適化。
  - 前面切替はタイトルバーだけを再合成し、Windowで覆われるタイトル背景stippleを省略。
  - damage region外の本文テキストをスキップ。
  - XOR枠の水平辺を整列済みlongアクセス化。

### 測定結果（cooperative、256色、`-bench 100`）

| phase | 初期 | 現在 | 備考 |
| :--- | ---: | ---: | :--- |
| full | 1020 | 1019 | 全画面は今回の主対象外 |
| region | 1100 | 65 | region clip/DMA正常化後 |
| z-expose | 1782 | 340 | region clip後 |
| drag-region | 554 | 314 | 43.3%減 |
| xor-move | 26 | 16 | long XOR化で38.5%減 |

- `drag-region`のGVRAM writeは `8878350 → 4634800`（47.8%減）。
- 16色モードはfull描画が遅く、drag-regionも256色より速くないため不採用。
- stippleのDMA化も遅いため不採用。
- ユーザーが最新XOR版で長時間ドラッグし、描画崩れがないことを確認済み。

### コミット

- `ed76af6 perf: optimize clipped drag rendering`
  - 本文スキップとXOR long化。
- `9f42868 perf: minimize drag title repair`
  - タイトル修復の最小化と固定drag-regionベンチ。
- それ以前のDMA・clip・runtime計測関連コミットもmainに含まれる。

### 検証済み

- `make test`: cooperative / preemptive 各74 PASS（計148）。
- `SS_PROFILE_GFX=1 make -C ssos/standalone standalone SCHED=cooperative -i`
- `SS_PROFILE_GFX=1 make -C ssos/standalone standalone SCHED=preemptive -i`
- 上記ビルドの最後の`/Users/scott/tmp`への`cp`はCodex sandboxで失敗するが、`ssos/standalone/ssos_cop.x`と`ssos_pre.x`のリンク成功は確認済み。ユーザー端末では`cp`で配置する。

### 作業ツリー

- READMEとこのhandoffを更新したため、次回開始時は未コミットの`README.md`と`.ai-handoff.md`を確認する。
- それ以外は`ed76af6`までコミット済み。

### 次回の具体的なステップ

1. 必要ならREADME/handoffの更新をコミットする（ユーザーが明示した場合のみ）。
2. 性能最適化は一旦停止する。
3. 継続する場合は、まず`next_z`が255を超えた後も前面化を保つz値正規化を実装・検証する。これは性能ではなく正しさ修正。
4. その後の性能実験は、通常経路を変更せず、HD63450アレイチェインDMAを実験用の切替として検証する。行stride・転送方向・DMAエラー時フォールバックを実機/エミュレータで個別確認する。
