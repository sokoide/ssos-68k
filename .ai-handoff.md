# AI Handoff

## 現状（2026-08-07：ホスト描画テスト・Timer D cadenceテスト拡張完了）

### 完了したこと

- X68000エミュレータ確認をハードウェア接続部分へ限定するためのテスト戦略を
  `docs/host-test-expansion-plan.md`へ整理した。
  - MFP全体の再実装ではなく、Timer Dイベントを決定論的に注入する方針。
  - Native framebuffer、m68k QEMU、X68000 emulator smoke、実機の責務を分離した。
- `SS_HOST_TEST`時だけGVRAM、CRTC、DMACレジスタをRAMへ差し替えるseamを追加した。
  - production buildでは従来の物理アドレスを使用する。
  - NativeではDMAC経路を無効化し、CPU rasterizationを検証する。
- `vram.c`を実際にRAM framebufferへ描画して検証する`test_gfx.c`を追加した。
  - mode選択、矩形clip、奇数座標、stipple、region intersection、glyph fast/slow、
    XOR矩形、page flip/CRTC scrollを画素単位で検証する。
- windowテストをgfx call counterから実framebufferの画素検証へ変更した。
- 描画テストで発見した不具合を修正した。
  - `ss_gfx_xor_rect()`が四隅を二重XORし、角が表示されない問題。
  - `ss_gfx_set_mode()`が未実装のmode 9〜15を受理する問題。
- QEMU preemptiveテストへ`t05_timerd_cadence`を追加した。
  - production同等の10 Timer D ticks周期を専用assembly objectで使用する。
  - 9 tickではcurrent task維持、10 tick目で`rte`経由切替を確認する。
  - tick 15期限のsleep taskが次のswitch tick 20で起床することを確認する。
  - QEMU `virt`にはMFPがないため、同期`trap #0`であり真の非同期preemptionではないことを文書化した。
- `verify-check.sh`の分類を更新した。
  - windowはNative framebufferでcovered。
  - gfxは画素処理をpartial coveredとし、実VRAM/CRTC/DMAC MMIOだけを手動確認対象にした。

### 検証済み

- `make test`: cooperative/preemptive各90、計180 PASS。
- `make test-qemu`: cooperative 4 + preemptive 5、計9 PASS。
- `make test-asm`: 5 PASS。
- クロスツールチェーンをsource後、以下すべてビルド成功。
  - `ssos_cop.x`
  - `ssos_pre.x`
  - `ssos_cop.xdf`
  - `ssos_pre.xdf`
- `git diff --check`: 問題なし。

### 進行中・未確認

- 自動検証とproduction buildは完了している。
- X68000エミュレータまたは実機で残る確認対象:
  - 実MFP Timer Dの非同期割り込み、vector、mask、EOI、周期。
  - 実GVRAM/CRTC MMIO、DMAC success/error/fallback。
  - IOCS keyboard/mouse、boot、終了時のハードウェア状態復元。
- QEMU Timer Dテストは同期`trap`なので、busy loopを任意命令位置で中断する性質は検証しない。

### 次回の具体的なステップ

1. `scene.c`のIOCS入力を小さなinput provider境界へ分離する。
2. 初期描画、content更新、window drag、ESCを複数frame実行するNative scene統合テストを追加する。
3. `render_region`とfull renderの同一領域を比較するdifferential testを追加する。
4. QEMUへ複数sleeper同時起床、priority、stack canary、task数上限stressを追加する。
5. X68000エミュレータでは4成果物の起動と上記ハードウェア境界だけをsmoke確認する。

今回の変更と本handoffは同一コミットに含める。pushは行わない。
