# Native unit tests - host clang, no cross toolchain.
#
# `make -f Makefile.native test` builds and runs the suite on the host. Pure
# logic modules (numfmt/buddy/slab) need no stubs; HW/asm dependencies are
# stubbed in framework/test_mocks.c (Phase 2 onwards).
#
# Scheduler variant: pass SCHED=preemptive to compile the matching wakeup
# policy. The scheduler core is shared. Default is cooperative.

SCHED ?= cooperative
ifeq ($(SCHED),cooperative)
  SCHED_DIR   = ../ssos/os/kernel/cooperative
  BUILD_DEF   = -DSS_BUILD_COOPERATIVE
else ifeq ($(SCHED),preemptive)
  SCHED_DIR   = ../ssos/os/kernel/preemptive
  BUILD_DEF   = -DSS_BUILD_PREEMPTIVE
else
  $(error Invalid SCHED='$(SCHED)'. Use: make -f Makefile.native SCHED=cooperative|preemptive)
endif

CC      = cc
CFLAGS  = -O2 -g -Wall -Wextra -Werror -std=c11 $(BUILD_DEF) -DSS_HOST_TEST -DSS_PROFILE_GFX=1 \
          -Iframework -I../ssos/os/util -I../ssos/os/mem \
          -I../ssos/os/kernel -I../ssos/os/win -I../ssos/os/gfx \
          -I../ssos/os/ipc

SSOS    = ../ssos/os
TARGET  = test_runner_native_$(SCHED)

# Keep instrumented runners separate so switching modes cannot reuse a stale
# binary. Stop at the first sanitizer finding, including undefined behavior.
ifeq ($(SANITIZE),1)
TARGET := $(TARGET)_sanitize
CFLAGS += -fsanitize=address,undefined -fno-sanitize-recover=all -fno-omit-frame-pointer
endif

# Pure-logic + host-seamed graphics + stubbed-HW sources. premain.c/main.c
# and the asm objects (entry.s, interrupts.s) remain excluded.
OS_SRCS = \
	$(SSOS)/util/numfmt.c \
	$(SSOS)/mem/buddy.c \
	$(SSOS)/mem/slab.c \
	$(SSOS)/kernel/scheduler.c \
	$(SCHED_DIR)/wakeups.c \
	$(SSOS)/kernel/work_queue.c \
	$(SSOS)/gfx/vram.c \
	$(SSOS)/gfx/profile.c \
	$(SSOS)/win/window.c \
	$(SSOS)/ipc/message.c

FRAMEWORK_SRCS = \
	framework/test_runner.c \
	framework/test_mocks.c

UNIT_SRCS = \
	unit/test_numfmt.c \
	unit/test_mem.c \
	unit/test_scheduler.c \
	unit/test_work_queue.c \
	unit/test_window.c \
	unit/test_gfx.c \
	unit/test_ipc.c

ALL_SRCS = $(FRAMEWORK_SRCS) $(UNIT_SRCS) $(OS_SRCS)
# This suite is compiled in one invocation; any shared header change must
# invalidate the runner just like a source change. Patterns that match
# nothing expand away, so reserve every include location up front: the
# tests root, unit/, and two levels under os/ (kernel/<sched>/ included).
HEADERS = $(wildcard *.h framework/*.h unit/*.h \
           $(SSOS)/*.h $(SSOS)/*/*.h $(SSOS)/*/*/*.h)

.PHONY: all test clean
all: $(TARGET)

test: $(TARGET)
	@echo "=== SSOS Native Tests (SCHED=$(SCHED)) ==="
	./$(TARGET)

$(TARGET): $(ALL_SRCS) $(HEADERS) Makefile.native
	$(CC) $(CFLAGS) $(ALL_SRCS) -o $(TARGET)

clean:
	rm -f test_runner_native_cooperative test_runner_native_preemptive
	rm -f test_runner_native_cooperative_sanitize test_runner_native_preemptive_sanitize
